# S4 — Article body link contrast (finding C050)

**Status:** Node invariant DONE (body link ≥4.5:1, verified). Page-level axe
criterion BLOCKED by one pre-existing out-of-scope violation — see Escalation.

## Change

Single rule in `src/app/globals.css`:

```css
.public-news-prose a {
-  color: #2d9fd6;
+  color: #066a99;
   font-weight: 800;
   text-decoration: underline;   /* unchanged — underline preserved */
   text-decoration-thickness: 2px;
   text-underline-offset: 4px;
}
```

Diff: `src/app/globals.css | 1 insertion(+), 1 deletion(-)`. No component files
touched. Byline color untouched. Underline rules untouched.

## RED → GREEN contrast math

Rendered prose background verified from the live page (not assumed):
computed `backgroundColor` of the link's painted ancestor =
`rgb(245, 251, 255)` = `#f5fbff` (matches `NewsArticle.tsx:22` `bg-[#f5fbff]`).

| State               | Color     | BG        | Ratio      | AA (≥4.5:1) |
| ------------------- | --------- | --------- | ---------- | ----------- |
| base (RED, before)  | `#2d9fd6` | `#f5fbff` | **2.86:1** | FAIL        |
| base (GREEN, after) | `#066a99` | `#f5fbff` | **5.70:1** | PASS        |
| hover               | `#111111` | `#f5fbff` | 18.1:1     | PASS        |

No `:visited` rule exists for `.public-news-prose a`, so base + hover are the
only variants; both pass. Reuses the byline-fix color `#066a99` (already proven
5.70:1).

## Gates

- **Repo gate:** `npx tsc --noEmit` → exit 0, no errors. (CSS-only; no unit tests.)
- **Browser gate (live probe):** the seeded article has no inline body anchors,
  so a representative `<a href="/news">` was injected into the live
  `.public-news-prose` at runtime via Playwright (no source change). Computed
  style confirmed: `color: rgb(6, 106, 153)` (= `#066a99`),
  `text-decoration-line: underline`, `font-weight: 800`,
  rendered background `rgb(245, 251, 255)`.
- **axe color-contrast:**
  - Prose-scoped (S4 node boundary, `.include(".public-news-prose")`):
    **0 serious/critical** violations.
  - Whole page: **1 serious** violation — see Escalation. Not the body link.

## Artifacts

- `plans/ux-verified-technical-fixes/agent-runs/s4-axe-article.json`
- `plans/ux-verified-technical-fixes/agent-runs/shots/s4-body-link.png`
- `plans/ux-verified-technical-fixes/agent-runs/shots/s4-article-full.png`
- `plans/ux-verified-technical-fixes/agent-runs/s4-axe.mjs` (repeatable script)

## Escalation — pre-existing out-of-scope violation

The one whole-page serious violation is NOT the body link:

- Element: `.mt-5` → the sidebar CTA subtitle "Get the complete A-Z blueprint
  to passive income." in `src/components/sections/NewsArticle.tsx:202`.
- `text-[#066a99]` on the dark `#111111` CTA card = **3.17:1** (fails AA).
- Pre-existing (git diff shows `NewsArticle.tsx` untouched by S4) and inside a
  component file, which S4's write scope explicitly forbids touching.

Suggested fix (needs scope expansion or a sibling node): the dark card's other
copy uses light tokens; the subtitle should match (e.g. a light blue like
`#9fe6ff` ≈ 9.4:1 on `#111111`, or white). One-token change in NewsArticle.tsx.

I did not make this change to respect the node boundary ("Must not touch: any
component file").

## Addendum — sidebar fix authorized & applied (team-lead, option a)

Team-lead expanded the write boundary to exactly the one token at
`src/components/sections/NewsArticle.tsx:202` (same C050 contrast family).

**Token choice:** reused the existing pattern rather than inventing a hex. The
dark CTA card (`bg-[#111111]`) already uses `text-white` for its own copy
(`NewsArticle.tsx:198`), and every `bg-[#111111]` surface across the codebase
uses `text-white` — there is no established light-blue-on-dark token. The
subtitle was the lone outlier.

Change:

```diff
- <p className="mt-5 text-lg leading-7 font-semibold text-[#066a99]">
+ <p className="mt-5 text-lg leading-7 font-semibold text-white">
```

| State  | Color     | BG        | Ratio    | AA (≥4.5:1) |
| ------ | --------- | --------- | -------- | ----------- |
| before | `#066a99` | `#111111` | 3.17:1   | FAIL        |
| after  | `#ffffff` | `#111111` | **21:1** | PASS        |

Live computed style after fix: subtitle `color: rgb(255, 255, 255)`.

**Re-verification (whole page):**

- `npx tsc --noEmit` → exit 0.
- axe color-contrast on the article page: **totalColorContrastViolations: 0,
  seriousOrCritical: 0** (script exit 0). `s4-axe-article.json` overwritten with
  passing result (before evidence retained in the RED→GREEN table above).
- New screenshot: `shots/s4-sidebar-cta.png` — white subtitle on dark card,
  legible.

Final source diff (entire round): 2 files, 1 line each —
`globals.css` (body link `#066a99`) + `NewsArticle.tsx:202` (subtitle `white`).
Nothing else in either file changed.

**S4 status: DONE.** All acceptance criteria met.
