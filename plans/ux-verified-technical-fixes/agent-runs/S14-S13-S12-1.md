# S14 → S13 → S12 worker report (PublicLeadForm.tsx chain)

Feature: ux-verified-technical-fixes. Branch: ux-review-fixes.
All three nodes write to `src/components/forms/PublicLeadForm.tsx`, executed in
dependency order. No git mutations performed.

## Files touched (within allowed scope)

- `src/components/forms/PublicLeadForm.tsx` (all three nodes)
- `src/components/forms/lead-error-summary.ts` (new — pure summary derivation, S14)
- `src/components/forms/lead-error-summary.test.ts` (new — S14 tests)
- `src/components/forms/PublicLeadForm.test.ts` (extended — S13 + S12 tests)
- `src/lib/content/us-states.ts` (new — extracted static array to keep main file
  from growing materially; matches components.md "content data in lib/content")
- `plans/ux-verified-technical-fixes/agent-runs/shots/{driver.mjs, s14-*.png, s13-*.png, s12-*.png}`

Did NOT touch: `lead-action-handler.ts`, `lib/services/leads.ts`, `progress.md`,
any other src file.

---

## NODE S14 — Apply form error summary (C014)

**Status: DONE.**

**RGR evidence.**

- RED: added `lead-error-summary.test.ts`. Pure-function tests passed
  immediately; the two render-level tests (summary present with
  `role="alert"`/`tabindex="-1"`, count copy, `href="#lead-..."` anchors)
  FAILED against the unmodified form — confirmed RED (2 failed / 5 passed).
- GREEN: extracted `deriveLeadErrorSummary(state)` (pure, ordered by field
  position), added a `LeadErrorSummary` subcomponent rendered at the top of the
  form when field errors exist, and a `useEffect` that focuses the summary
  (`ref` + `tabIndex={-1}`) whenever it appears or re-appears (keyed on the
  failed-field signature). All 7 summary tests pass.

**Acceptance criteria.**

- Summary box at top: "There are N problems with your application" (plural) /
  "There is 1 problem with your application" (singular). Browser: rendered
  "There are 6 problems with your application" on empty apply submit.
- One anchor per failed field, `href="#lead-<name>"`, text "Label: message".
- Activating an anchor focuses the matching input — browser-proven: clicking
  the first anchor (`#lead-full_name`) set `document.activeElement.id` to
  `lead-full_name`.
- Focus moves to the summary on appearance — browser-proven: active element
  carried `role="alert"` immediately after submit.
- No duplicate SR spam: the summary is the single assertive source
  (`role="alert"`, focused). The existing bottom region (`ActionMessage`) is
  demoted to polite/no-role via a new `muted` prop WHEN field errors exist; it
  keeps its assertive role for server-errors-without-field-errors and stays
  polite for success. Success/redirect/panel announcements unchanged.

**Browser gate:** `shots/s14-summary.png` (summary + per-field inline errors
both visible), `shots/s14-anchor-focus.png` (after anchor activation). Driver
logged: `summary focused on appearance: true`, `anchor #lead-full_name ->
focused element id: lead-full_name`. No valid submission (empty form fails
server validation, stores nothing).

---

## NODE S13 — Privacy line at point of submission (C088)

**Status: DONE.**

**RGR evidence.** RED: two tests (apply + contact) asserting `href="/privacy"`,
"We never sell your data", and per-formType lead copy — FAILED on the
unmodified form. GREEN: added `PrivacyAssurance` subcomponent below the submit
row using `next/link` to `/privacy`.

**Route verification:** privacy page exists at `src/app/privacy/page.tsx` →
canonical href `/privacy` (no route group affecting the URL). Browser driver
confirmed the rendered link href is `/privacy`.

**Copy.** Apply: "By applying you agree to our Privacy Policy. We never sell
your data." Contact: "By sending this note you agree to our Privacy Policy. We
never sell your data."

**Browser gate:** `shots/s13-privacy.png` (line near submit on apply); also
visible at the bottom of `s14-summary.png`.

---

## NODE S12 — "(optional)" labels (C027)

**Status: DONE.**

**RGR evidence.** RED: test asserting contact State shows "(optional)", Phone
shows "(optional)", required fields keep "(required)", apply State keeps
"(required)", and apply message shows "(optional)" — FAILED (non-required
fields previously rendered no marker at all). GREEN: extracted a shared
`FieldLabel` subcomponent used by `TextField`/`SelectField`/`TextareaField`;
required fields keep the `*` + sr-only "(required)", non-required fields get a
visible muted "(optional)" suffix.

**Behavior.** Contact State → "State (optional)" (browser-confirmed exact text).
Apply State → stays required (`*`). Apply message ("What are you trying to
build?") → optional. Contact message → stays required (matches existing
server-side `required={!isApply}`).

**Browser gate:** `shots/s12-optional.png` (contact State "(optional)"); also
visible in `s14-summary.png` (Phone/City/message optional, required fields
asterisked).

---

## Gates (run after full chain)

- **Repo Gate (tests):** `npx vitest run src/components/forms
src/app/lead-action-state.test.ts` → 3 files, 20 tests, all pass.
- **Repo Gate (tsc):** `npx tsc --noEmit` — zero errors in any file I touched
  (`PublicLeadForm.tsx`, `lead-error-summary.ts`, `us-states.ts`, the test
  files). The only repo-wide tsc errors observed are in files owned by other
  concurrent workers (`src/app/admin/pages/page.test.ts`, transiently
  `NewsEditorForm.tsx`) — not in scope and not caused by this chain.
- **File size:** `PublicLeadForm.tsx` is 538 lines. It was already 492 lines
  (over the 300 guideline) before this work. Net of three features added, I
  extracted three modules (`lead-error-summary.ts`, `FieldLabel`, `us-states.ts`)
  so the increase is modest (+46) and duplication was reduced (3 label blocks →
  1). Not made materially worse.
- **Browser Gate (:3001):** all three required captures produced; assertions
  logged and pass. No valid lead submitted.

## Behavior preservation

- Conversion path intact: success still redirects (apply) / shows
  `ContactSuccessPanel` (contact) — `resolveLeadSuccessTransition` and the
  redirect effect untouched.
- Per-field inline errors, `aria-invalid`/`aria-describedby` wiring unchanged
  (visible in `s14-summary.png`).
- "Not sure yet" options still present (existing test still green).
- Hooks added (`useRef`, `useEffect`) are declared before the early panel
  return — no conditional-hook violation.

## Residual risk

- Low. Summary anchor text repeats the field label and the server message
  (e.g. "Business stage: Business stage is required") which is slightly
  redundant but unambiguous; not a defect.
- The focus-on-appearance effect re-fires on each still-invalid resubmit (keyed
  on the failed-field signature) — intended; harmless if the same fields fail
  again because the signature is identical and the effect is idempotent.
- DOM-level focus assertions are proven via the Playwright browser gate (the
  repo's unit tests are SSR-only with `environment: node`, no jsdom/RTL
  installed — matching the existing pattern that defers redirect behavior to
  the browser gate). No new test dependency was introduced.

## Recommendation

- S14: ship.
- S13: ship.
- S12: ship.
