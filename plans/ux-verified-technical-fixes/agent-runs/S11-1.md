# S11 — Content libraries sidebar entry (finding C142)

## Goal

Make `/admin/libraries` reachable from the admin sidebar, not only via the
Media library header link. Add a "Content libraries" entry to the sidebar's
`contentSections` array, mirroring the prior `blogSection` fix.

## Changes

- `src/components/admin/AdminShell.tsx` — appended a new `AdminNavSection` to
  `contentSections` after Media library:
  - `id: "libraries"` (the `AdminSection` type already includes this member)
  - `label: "Content libraries"`
  - `href: "/admin/libraries"`
  - `description: "Reusable content collections"`
  - `icon: "archive"` (already defined in `AdminIconGlyph`)
    No new types, icons, or render code needed — the section flows through the
    existing desktop nav group, mobile nav list, and active-label logic.
- `src/components/admin/AdminShell.test.tsx` — added two tests mirroring the
  existing blog tests (link present + href; active state via `aria-current`).

## Active-state model (confirmed)

Active state is driven by the `activeSection` prop, not URL prefix matching.
Server pages set it explicitly; `src/app/admin/libraries/page.tsx:41` already
passes `activeSection="libraries"`. The sidebar item highlights on exact
`activeSection === section.id` equality — identical to all siblings. No prefix
logic exists anywhere, so nothing to mirror there.

## Loop evidence

### RED

Added the two tests first. Run failed as expected:

- `expected html to contain 'href="/admin/libraries"'` (link absent)
- `expected null not to be null` (no active libraries link)
  2 failed | 3 passed.

### GREEN

Added the `contentSections` entry. Repo gate:

- `npx vitest run src/components/admin/AdminShell.test.tsx` → 5 passed (5).
- `npx tsc --noEmit` → clean, no output.

### REFACTOR

None needed — single data-array addition.

### Browser gate (:3001, dev-auth bypass)

Playwright script `shots/s11-shot.mjs`:

1. Loaded `/admin/media`, screenshotted the sidebar showing the new
   "Content libraries" item under CONTENT →
   `shots/s11-sidebar-item-present.png`.
2. Clicked the sidebar "Content libraries" link (scoped to `aside` because the
   Media library page header also carries a matching C142 link).
3. Navigated to `/admin/libraries`, screenshotted the sidebar with the item
   active → `shots/s11-libraries-active.png`.
4. Asserted `url === .../admin/libraries` and the sidebar link's
   `aria-current === "page"`. Both confirmed.

## Acceptance criteria

- [x] Sidebar link renders with appropriate icon/label consistent with neighbors
      (archive icon, same nav-item markup).
- [x] Active state on `/admin/libraries` (exact `activeSection` match, matching
      sibling behavior; no prefix matching exists to mirror).
- [x] Existing sidebar items unchanged (3 prior nav tests stay green; visual
      style untouched).

## Boundary compliance

Only touched `src/components/admin/AdminShell.tsx`,
`src/components/admin/AdminShell.test.tsx`, and report/screenshot artifacts
under `plans/`. Did not touch `progress.md` or any other `src/` file.

## Verdict

PASS.
