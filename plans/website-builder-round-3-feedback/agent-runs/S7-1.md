# S7 — Document importer visible truncation warning (I7/R3-7)

**Status:** COMPLETE — all gates pass.

## Files changed

- `src/lib/page-builder/document-import.ts` — truncation warning now states the exact dropped count: `"{n} section(s) dropped — only the first 8 were imported."` (was the count-less `"Only the first 8 sections were imported."`). No warning at or under the cap (unchanged behavior, now count-guarded via `droppedCount > 0`).
- `src/lib/page-builder/document-import.test.ts` — 3 new tests: 10 sections → exact 2-dropped message; 9 sections → singular wording; 8 sections → no warning, all 8 blocks imported.
- `src/components/admin/seo-page-editor/AiBuilderAssistant.tsx` — two minimal edits in the DocumentImportPanel/DocumentImportReview region: (1) warning `<p>` in `DocumentImportReview` now carries `role="status"` (amber alert styling already existed), so the truncation message is a live region announced before insertion; (2) `DocumentImportPanel` exported so it can be render-tested.
- `src/components/admin/seo-page-editor/document-import-panel-warning.test.tsx` — NEW panel render test using the repo's `renderToStaticMarkup` idiom (same pattern as `block-toolbar-aria.test.tsx`). Renders `DocumentImportPanel` with a real 10-section proposal and asserts the `role="status"` paragraph with the exact dropped-count text; under-cap render asserts no `role="status"` and no "dropped" text.

## Adaptation note (per node context)

A `warnings: string[]` field already existed on `DocumentImportProposal` and `DocumentImportReview` already rendered it — so the gap was (a) the message lacked the dropped count and (b) the element had no live-region role. The fix targeted exactly those two gaps; no new plumbing was needed. The panel was statically renderable (only `useState`/`useMemo`, no effects needed for initial markup), so a real render test was feasible — no fallback to lib-only coverage required.

## RED evidence

`npm run test -- src/lib/page-builder/document-import.test.ts src/components/admin/seo-page-editor/document-import-panel-warning.test.tsx` before the implementation change:

```
Test Files  2 failed (2)
     Tests  4 failed | 3 passed (7)
```

- Lib: expected `"2 sections dropped — only the first 8 were imported."`, received `"Only the first 8 sections were imported."` (and singular variant).
- Panel: `Element type is invalid ... got: undefined` (DocumentImportPanel not exported) — both panel tests failed.

## GREEN evidence

Same command after the minimal changes:

```
Test Files  2 passed (2)
     Tests  7 passed (7)
```

## REFACTOR summary

None needed — the GREEN change was already minimal (one warning-construction expression, one `role` attribute, one `export` keyword). No duplication introduced.

## Repo gate results

- `npm run test -- src/lib/page-builder/document-import.test.ts src/components/admin/seo-page-editor/document-import-panel-warning.test.tsx` → 7/7 PASS
- `npm run typecheck` (`tsc --noEmit`) → PASS, no errors
- `npx eslint` on all 4 changed files → PASS, no output
- `npm run test -- src/components/admin/seo-page-editor` → 27 files, 129 tests, all PASS (no editor-suite regression)

## Behavior preservation

- Under/at-cap imports unchanged: new test "emits no warning when the document is at or under the import cap" proves 8 sections → 8 blocks, `warnings: []`; existing 2-section mapping test and unsafe-link test still pass unchanged.
- Over-cap imports still cap at 8 blocks (asserted in the new over-cap test); only the warning text changed.
- Panel: under-cap render produces no `role="status"` element, so no spurious live-region announcements for clean imports.

## Residual risk

- Low. The exact warning string is asserted in two test files; any future copy change must update both (intentional — the string is the user-facing contract for this issue).
- `DocumentImportPanel` is now exported from `AiBuilderAssistant.tsx` solely for testability; it is not imported by any other production module.
- Browser gate (visual confirmation of the amber status box in the live editor) is owned by the orchestrator — not run here per node constraints.

## Recommendation

Accept. Orchestrator browser gate should paste a >8-section document into the import panel and confirm the amber "N sections dropped — only the first 8 were imported." box appears above the block checklist before insertion.
