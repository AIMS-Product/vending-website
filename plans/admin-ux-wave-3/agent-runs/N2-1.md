```
CONTRACT: HC-admin-dashboard-1
STATUS: DONE
FILES CHANGED:
- src/lib/services/admin-overview.ts (new)
- src/lib/services/admin-overview.test.ts (new)
- src/app/admin/page.tsx (new)
- next.config.ts (removed /admin -> /admin/pages redirect only)
- plans/admin-ux-wave-3/agent-runs/N2-1.md (new, this report)

RED EVIDENCE:
- Ran `npx vitest run src/lib/services/admin-overview.test.ts` before the
  service existed: failed with
  "Error: Cannot find module './admin-overview' imported from
  .../src/lib/services/admin-overview.test.ts" (1 failed suite, 0 tests).

GREEN EVIDENCE:
- After implementing src/lib/services/admin-overview.ts:
  `npx vitest run src/lib/services/admin-overview.test.ts` ->
  "Test Files  1 passed (1)" / "Tests  4 passed (4)".
- Re-ran again after `prettier --write` reformatted both files (whitespace
  only) to confirm no regression: still 1 passed / 4 passed.

GATES:
- tsc: `npx tsc --noEmit` — only pre-existing failure is
  src/app/admin/libraries/LibrariesWorkspace.test.tsx (missing module),
  which belongs to a parallel worker's untracked files, not this contract.
  Verified by `git stash` (removing all my changes) and re-running tsc:
  identical single error persists, confirming it predates and is unrelated
  to this change. Zero tsc errors from my files.
- eslint: `npx eslint src/app/admin/page.tsx src/lib/services/admin-overview.ts
  src/lib/services/admin-overview.test.ts next.config.ts` — no output, clean.
- prettier: `npx prettier --write <same files>` — admin-overview.ts and
  admin-overview.test.ts reformatted (whitespace only); page.tsx and
  next.config.ts already formatted (unchanged). Re-ran targeted vitest after
  to confirm still green.
- vitest (targeted): `npx vitest run src/lib/services/admin-overview.test.ts`
  — 1 file passed, 4 tests passed.

CRITERIA:
- [x] getAdminOverview returns all nine counts read-only, unit-tested incl.
      7-day window and failed-sync set {failed, needs_review, dead_letter}.
      Evidence: 4 passing tests covering the full shape, the 7-day boundary
      (inclusive at exactly now-7d, exclusive one ms before), the 3-status
      failed-sync set, and scheduled-publish pending/failed counted
      separately. All 9 queries use `.select("id", { count: "exact", head:
      true })` — no row data is ever fetched, no writes performed.
- [x] /admin page compiles as server component using requireAdmin +
      AdminShell, sections per spec, needs-attention hidden at zero.
      Evidence: src/app/admin/page.tsx is an async Server Component (no
      "use client"), calls `requireAdmin()` and `getAdminOverview()` via
      Promise.all, renders inside AdminShell exactly like leads/page.tsx.
      buildNeedsAttentionItems() only pushes an item when its count > 0;
      the "All clear" quiet line renders when the resulting array is empty.
      Content-live strip links to /admin/pages and /admin/news; leads
      section links to /admin/leads; quick actions link to /admin/pages/new,
      /admin/news/new, /admin/media.
- [x] next.config.ts redirect removed, nothing else changed in it.
      Evidence: only the { source: "/admin", destination: "/admin/pages",
      permanent: false } block was deleted; `/login` redirect and all other
      entries are untouched (verified by reading the diff — only that block
      is missing).
- [x] tsc/eslint/prettier/targeted vitest pass. See GATES above.

DEVIATIONS: None from the spec. Two implementation choices worth flagging
as judgment calls within the allowed scope:
1. leadsTotal is computed via an unfiltered count query against
   lead_submissions (`(query) => query`) rather than a separate code path —
   this is a plain head:true count with no filters applied, still
   read-only, still a single query, just expressed via the same
   countRows() helper as every other metric for consistency.
2. The component test for the page was skipped per the contract's explicit
   opt-out ("static-markup test for the page component is optional — if
   the page is a thin async component, test the service thoroughly
   instead and note it"). AdminOverviewPage is a thin composition of
   requireAdmin + getAdminOverview + AdminShell + AdminUi primitives with
   one small pure helper (buildNeedsAttentionItems), so I tested the
   service exhaustively (4 tests, all 9 fields, boundary conditions, both
   attention-triggering paths) and left the page untested, noting it here
   as instructed.

NOTES:
Table/column names used (confirmed by reading src/types/database.ts,
src/lib/services/seo-pages.ts, src/lib/services/news.ts,
src/lib/services/seo-page-scheduler.ts, and
src/components/admin/AdminLeadsManager.tsx):
- seo_pages.status ("published" / "draft") for pagesPublished/pagesDraft.
- seo_pages.scheduled_publish_status ("scheduled" / "failed") for
  scheduledPublishPending/scheduledPublishFailed (values confirmed via
  grep against seo-page-scheduler.ts and seo-pages.ts usage:
  "scheduled" | "published" | "failed" | "cancelled").
- news_posts.status ("published" / "draft") for
  postsPublished/postsDraft.
- lead_submissions.created_at (ISO timestamp) for leadsThisWeek (>=
  now - 7 days, computed in JS via `new Date(now().getTime() -
  SEVEN_DAYS_MS).toISOString()`) and leadsTotal (unfiltered count).
- close_sync_events.status with the failed-sync set
  {"failed", "needs_review", "dead_letter"} for failedSyncs — this exact
  set and table name is lifted directly from
  AdminLeadsManager.tsx's `retryableStatuses` /
  `RETRYABLE_EVENT_STATUSES` (also cross-checked against
  lead-admin.ts's own `RETRYABLE_EVENT_STATUSES` constant) rather than
  reading lead_submissions.close_sync_status, since the review spec's
  "failed Close syncs" language maps to the sync-event queue, matching
  the existing UI's own definition of a stuck sync.

activeSection value chosen: "pages" (the AdminShell activeSection union in
src/components/admin/AdminShell.tsx has no neutral/overview value — it is
"pages" | "posts" | "forms" | "leads" | "media" | "libraries" | "settings" |
"routes" — so per the contract's fallback instruction I used the pages
section value, since /admin's content-live section leads with pages, and
left a code comment in src/app/admin/page.tsx noting node N5 will add a
dedicated Overview section to AdminShell). AdminShell.tsx itself was not
touched, per the Forbidden list.
```

---

# Attempt 2 — redispatch fix (failedSyncs semantics)

```
CONTRACT: HC-admin-dashboard-1 (attempt 2)
STATUS: DONE
FILES CHANGED:
- src/lib/services/admin-overview.ts (failedSyncs now counts lead_submissions
  filtered by close_sync_status IN {failed, needs_review, dead_letter};
  comment updated to say it counts LEADS stuck in a failed sync state,
  matching AdminLeadsManager's failedSyncCount; "close_sync_events" dropped
  from the countRows table union — no longer queried)
- src/lib/services/admin-overview.test.ts (tests updated to drive the
  corrected table/column and assert leads-not-events semantics; removed the
  unused `lte` helper from the fake)
- plans/admin-ux-wave-3/agent-runs/N2-1.md (this appended section)
No other files changed.

RED EVIDENCE:
- Updated the tests FIRST against the events-based implementation:
  `npx vitest run src/lib/services/admin-overview.test.ts` → 2 failed |
  2 passed. Failures reproduced the exact violation semantics:
  - Overview test: 1 stuck lead (close_sync_status dead_letter) with 3
    failed close_sync_events rows behind it → expected failedSyncs 1,
    received 3 (the events count).
  - Failed-sync-set test: 3 stuck leads across the status set with 5
    failed event rows as a decoy → expected 3, received 5.

GREEN EVIDENCE:
- After switching the failedSyncs query to
  `countRows(client, "lead_submissions", (q) =>
    q.in("close_sync_status", [...FAILED_SYNC_STATUSES]))`:
  `npx vitest run src/lib/services/admin-overview.test.ts` →
  "Test Files  1 passed (1)" / "Tests  4 passed (4)".

GATES:
- tsc: `npx tsc --noEmit` → zero errors repo-wide (the previously noted
  pre-existing LibrariesWorkspace error has since been resolved by its
  owning worker; nothing to filter anymore).
- eslint: `npx eslint src/lib/services/admin-overview.ts
  src/lib/services/admin-overview.test.ts` → no output, clean.
- prettier: `npx prettier --write <both files>` → both "(unchanged)".
- vitest (targeted): 1 file passed, 4 tests passed.

CRITERIA:
- [x] failedSyncs now counts LEADS via lead_submissions.close_sync_status
      with the set {failed, needs_review, dead_letter} — the same column,
      set, and row-granularity as AdminLeadsManager.tsx:105's
      failedSyncCount, so the dashboard needs-attention number equals the
      leads page banner number by construction.
- [x] Tests assert the leads-not-events semantics with decoy
      close_sync_events state: 1 stuck lead / 3 failed event rows →
      failedSyncs === 1; 3 stuck leads / 5 failed event rows →
      failedSyncs === 3. A regression back to counting events flips both
      assertions.
- [x] Repo gate re-run and passing (tsc / eslint / prettier / vitest).

DEVIATIONS: None. One tidy-up inside allowed files: removed the fake's
unused `lte` helper (dead code from attempt 1) while updating the test.

NOTES: The lead row test data includes close_sync_status null (never
attempted) alongside all six status values to pin the filter to exactly
the three-failure set. The close_sync_events table remains in the fake
state as a decoy only — the service no longer queries it, and its table
union is now "seo_pages" | "news_posts" | "lead_submissions".
```
