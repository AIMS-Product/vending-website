CONTRACT: HC-adminshell-landmark-2
STATUS: DONE
FILES CHANGED:

- src/components/admin/AdminShell.tsx (aside-to-div swap + constraint comment; structural only)
- src/components/admin/AdminShell.landmarks.test.ts (new, source-scan style)
  RED EVIDENCE: `npx vitest run src/components/admin/AdminShell.landmarks.test.ts` against the unmodified source failed for exactly the intended reason — "does not render the desktop sidebar as an aside landmark" failed on `expect(source).not.toMatch(/<aside[\s>]/)` (Test Files 1 failed; Tests 1 failed | 2 passed). The other two assertions (no nested main; sidebar nav retained) already passed, matching the contract's predicted failure mode.
  GREEN EVIDENCE: after the swap, `npx vitest run` on the new test plus all five existing test files referencing AdminShell: Test Files 6 passed (6), Tests 28 passed (28). Files run: AdminShell.landmarks.test.ts, AdminShell.test.tsx, src/app/admin/pages/[id]/source/page.test.tsx, AdminLeadsManager.test.tsx, AdminLeadsManager.landmarks.test.ts, NewsEditorForm.landmarks.test.ts.
  GATES: tsc exit 0 | eslint exit 0 (both changed files) | prettier exit 0 (AdminShell.tsx unchanged by formatter; test file reformatted — readFileSync call wrapped, cosmetic only) | vitest 6 files / 28 tests, all passing
  CRITERIA:
- Desktop sidebar renders as div with identical classes/children; nav landmark retained — MET: `git diff` shows only the opening tag `<aside className="...">` -> `<div className="...">` (className string byte-identical), closing tag swap, and the comment block; `<nav aria-label={ariaLabel}>` with `ariaLabel="Admin sections"` untouched.
- Landmarks test RED-then-GREEN evidence — MET (see above; RED failed only on the aside assertion, GREEN all pass).
- No other behavior/visual change in the diff — MET: diff is 9 insertions / 2 deletions, all within AdminDesktopSidebar's return: 7 comment lines + the two tag lines. No class, prop, child, or logic changes anywhere else.
- tsc/eslint/prettier/vitest pass — MET (all exit 0 / all green).
  DEVIATIONS: none.
  NOTES:
- Spec item 2 landmark sweep of AdminShell.tsx: the line ~278 sidebar aside was the ONLY complementary/main-class nested landmark. Two other candidates were checked and deliberately left alone: (a) line ~552 `header className="mb-5"` — it is a descendant of the AdminShellContent section element, so per the HTML-AAM scoping rule it never maps to the banner role and is not a landmark; (b) line ~539 the `section aria-labelledby="admin-shell-title"` — a named region landmark, which axe permits nested inside main (top-level rules apply only to complementary/banner/contentinfo), and it is not in this contract's violation class. Neither was changed.
- No file, e2e test, or selector anywhere in the repo targets the sidebar via an `aside` selector (grepped src/ and e2e/); the tag swap has no test or styling blast radius.
- The constraint comment in AdminShell.tsx and the test docblock are both worded without literal angle-bracket tag text, so the source-scan regexes (`/<aside[\s>]/`, `/<main[\s>]/`) cannot false-fail on comments — the AdminLeadsManager gotcha documented in the contract.
- The test also pins `ariaLabel="Admin sections"` + the `<nav aria-label={ariaLabel}` render site (the literal `aria-label="Admin sections"` string does not exist in source — it flows through the ariaLabel prop — so the source-scan asserts both the prop value and the nav that consumes it).
- Browser axe verification on live /admin routes remains a feature-gate step (this suite is node-env source-scan only, per repo test conventions).
