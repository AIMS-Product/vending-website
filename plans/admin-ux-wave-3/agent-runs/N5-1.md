CONTRACT: HC-overview-nav-1
STATUS: DONE
FILES CHANGED:

- src/components/admin/AdminShell.tsx ("overview" AdminSection + "list" in the AdminShellIconName Extract + Overview entry first in contentSections)
- src/components/admin/AdminShell.test.tsx (2 new tests: Overview first in both navs; Overview active on /admin)
- src/app/admin/page.tsx (activeSection "pages" -> "overview"; placeholder N5 comment deleted — nothing else touched)
  RED EVIDENCE: `npx vitest run src/components/admin/AdminShell.test.tsx` against unmodified AdminShell failed on exactly the 2 new tests — "renders the Overview link first in both mobile and desktop Content navs" (expected overviewLinks toHaveLength(2), got 0) and "marks Overview as the active page when on /admin" (expected match not to be null). Tests 2 failed | 11 passed (13) — all pre-existing tests unaffected.
  GREEN EVIDENCE: after the nav entry was added: AdminShell.test.tsx + AdminShell.landmarks.test.ts — Test Files 2 passed, Tests 16 passed. Full sweep (those two + src/app/admin/pages/[id]/source/page.test.tsx + AdminLeadsManager.test.tsx + AdminLeadsManager.landmarks.test.ts + NewsEditorForm.landmarks.test.ts): Test Files 6 passed (6), Tests 30 passed (30).
  GATES: tsc exit 0 | eslint exit 0 (all three changed files) | prettier exit 0 (all three unchanged by formatter) | vitest 6 files / 30 tests, all passing
  CRITERIA:
- Overview entry first in nav, href /admin, both desktop and mobile — MET: entry is element 0 of contentSections, which both AdminMobileNavList (mobile) and AdminDesktopNavGroup (desktop) consume; test asserts exactly 2 `href="/admin"` occurrences and that each precedes its nav's /admin/pages sibling (indexOf covers mobile, lastIndexOf covers desktop, since mobile renders before desktop).
- Dashboard page highlights Overview as active; placeholder comment gone — MET: page.tsx diff is exactly the 3 comment lines removed + activeSection="pages" -> "overview"; active-state test proves aria-current="page" lands on the /admin link when activeSection="overview".
- RED-then-GREEN evidence; tsc/eslint/prettier/vitest pass — MET (see above).
  DEVIATIONS: none.
  NOTES:
- Icon: no home/dashboard/gauge glyph exists in AdminUi's AdminIconName set (archive, book, check, crown, file, filter, help, image, layers, list, log-out, mail, more, newspaper, pencil, plus, save, search, settings, shield, target, trash, upload). Chose "list" — an at-a-glance summary glyph, unused by any other sidebar entry. "layers" was the other candidate but is already used by Route prefixes; duplicating an icon within the same sidebar would be confusing. No icons were added to AdminUi.tsx.
- Description "Studio overview" matches the sibling copy pattern and the page's own eyebrow text.
- "list" also had to be added to AdminShell's local AdminShellIconName Extract union (it narrows AdminIconName to the icons the shell may use) — required for the entry to type-check; no change to AdminUi.
- Test technique note: the substring `href="/admin"` (including the closing quote) cannot match sibling hrefs like `href="/admin/pages"`, so occurrence counting is exact. Verified nothing else in the shell renders an /admin link (brand block and mobile summary are not links).
- AdminShell.landmarks.test.ts needed no changes (it scans for landmark tags and the sidebar nav, none of which this change affects) — it was run in the sweep and passes.
- No test pins the dashboard page's activeSection (src/app/admin/pages/[id]/source/page.test.tsx mocks AdminShell; admin-overview.test.ts covers the service), so the page.tsx change has no test blast radius.
- Browser verification of /admin highlighting remains a feature-gate step (dev server/browser forbidden in this contract).
