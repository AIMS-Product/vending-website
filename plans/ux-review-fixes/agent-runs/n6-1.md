# N6 — Redirects discoverability + row-menu verification + test-page archive

## Status

COMPLETE — all three parts verified. Row "⋮" actions menu works for real interaction (opens on a normal `.click()`), answering the review's open verification question.

## Files changed (code)

- `src/app/admin/pages/page.tsx` — added a "Redirects" secondary header link beside "Create page" (only `AdminPagesActions` touched).
- `src/app/admin/pages/page.test.ts` — added a render test asserting the link href + label.

No other files modified. `PageActionsMenu` and all list behavior (filters, summary cards, search, row actions) left unchanged.

## Part A — Redirects discoverability (RGR)

**RED:** Added test `exposes a Redirects link in the list header actions` asserting `href="/admin/pages/redirects"` and `>Redirects</a>` in the rendered header. Failed as expected (link absent).

**GREEN:** Added a `<Link href="/admin/pages/redirects">` using `adminSecondaryButtonClass` (already imported) inside `AdminPagesActions`, placed before the primary "Create page" button. Changed the primary button from `w-full` to `flex-1` so the two share the header row at equal `h-11` height.

**REFACTOR:** None needed — single new element, reuses the existing secondary-button token. Design-contract aligned: the redirects page itself uses `adminSecondaryButtonClass` for its "Back to pages" link, so this is the symmetric secondary-header-action pattern. No raw schema labels, no overflow, keyboard-focusable Link.

### Repo gate

- `npx vitest run src/app/admin/pages/page.test.ts src/app/admin/pages/actions.test.ts` → 31/31 pass (my scope).
- Full `npx vitest run` → 403 pass, 4 fail. The 4 failures are in **other workers' in-progress files** (`src/components/forms/PublicLeadForm.test.ts`, `src/components/sections/NewsHero.test.tsx`) — NOT in my write scope and unrelated to the pages list. Confirmed via `git diff --name-only` (those files are modified by sibling N5/N7 work).
- `npx tsc --noEmit` → one error, in `src/components/forms/PublicLeadForm.test.ts` (another worker's file). Zero errors in `admin/pages/page*`.
- `npx eslint src/app/admin/pages/page.tsx src/app/admin/pages/page.test.ts` → clean (exit 0).

### Browser gate

Shared dev server on :3000 was returning 500 then hanging (000) on ALL routes including `/` — a broken Turbopack cache + duplicated `next dev` instances (environmental, pre-existing, affects routes that don't touch my files). I did NOT restart the shared server (other workers depend on it). Instead ran an isolated `next dev -p 3210`, which returned **200** on `/admin/pages` with admin bypass active — proving my code renders fine and the :3000 failure is not mine.

- `n6-a1-link-on-list.png` — "Redirects" secondary button visible left of "+ Create page" in the header.
- `n6-a2-redirects-page.png` — click-through lands on the Redirects manager (h1 "Redirects").

## Part B — Row "⋮" actions menu (the review's open question)

**The menu DOES open with a normal Playwright `.click()` on the `<summary>`.** No force-click or keyboard fallback was needed. This resolves the review's "automation clicks failed" uncertainty — the native `<details>/<summary>` menu is fully interactive for a real browser user.

- Open-method used: `summary.click()` (method 1, first try).
- Open menu items read from DOM: **Edit page, Duplicate page, Publish page, Archive page** (matches source).
- Evidence: `n6-b1-menu-open.png`, and `n6-c2-confirm-dialog.png` shows the open menu (Archive page in red) alongside the confirm modal in one frame.

## Part C — Archive "UX Review Test Page 1781052712783"

Archive uses a **custom `<dialog>` confirm modal** (not native `confirm()`): clicking "Archive page" opens a modal with Cancel/Confirm. Clicked Confirm.

- Before: page present in Drafts (Drafts count 5).
- `n6-c2-confirm-dialog.png` — modal "Confirm archive page" / "Archive \"UX Review Test Page 1781052712783\"? This removes it from the active page list."
- After Confirm: page **left the draft list (count 0 for that title)**; Drafts 5 → 4.
- `n6-c4-archived-filter.png` — page **appears under the Archived filter**; Archived count 2 → 3.

Only the target page was archived. No Publish clicked on anything. No other page touched.

## Behavior preservation

`PageActionsMenu`, filters, summary cards, search, sort, pagination all unchanged. The only edit to the list page is the additive header link + a `w-full`→`flex-1` class swap on the existing primary button. Full existing `page.test.ts` governance test still passes.

## Residual risk

- Low. The added Redirects link is a static `Link`; no data, no state, no new dependency.
- The shared :3000 dev server is in a broken/hung state (environmental — broken Turbopack cache, duplicate `next dev` PIDs 56352 & 93860). Not caused by N6, but worth a clean restart of the shared dev environment by whoever owns it; my isolated :3210 instance was stopped after use.

## Recommendation

Merge N6 as-is. The row actions menu is confirmed working for real interaction, so the review's flagged uncertainty is closed — no bug there.
