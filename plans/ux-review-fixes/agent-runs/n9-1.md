# N9 — Mobile horizontal scroll + article H1 overflow

## Status

COMPLETE — all 3 reproduced overflows fixed; all 7 target pages pass at 375px; full repo + browser gates green; desktop unchanged.

## Core invariant

Desktop layouts unchanged; copy unchanged. Verified by 1280px screenshots and zero content edits.

## Per-page reproduction table (reproduce-first)

| Page (375x667)                                | Reviewer flag                                                             | Reproduced?    | Root culprit                                                                                                                                                                                            | Fix                                                                       |
| --------------------------------------------- | ------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `/`                                           | horizontal scroll (overflow 30px)                                         | YES            | CSS Grid blowout: Benefits grid item `div.flex.h-full.flex-col` had default `min-width:auto`, grew to its intrinsic content width (385px) inside a 335px grid track                                     | `min-w-0` on the grid item + `break-words` on its `<h2>` (`Benefits.tsx`) |
| `/case-studies`                               | horizontal scroll (overflow 23px)                                         | YES            | Same grid-blowout: quote-card `<li>` grid items had `min-width:auto`, content (378px) exceeded the single-column mobile track                                                                           | `min-w-0` on the `<li>` (`CaseStudyQuotes.tsx`)                           |
| `/news/...vending-entrepreneurship-program`   | (found via sweep) article H1 long-word overflow (overflow 104px → sw 479) | YES            | H1 `overflow-wrap:normal` could not break the 16-char uppercase word "ENTREPRENEURSHIP" at the 44px clamp-min font size; text painted 124px past its box                                                | `break-words` on the article `<h1>` (`NewsArticle.tsx`)                   |
| `/news`                                       | horizontal scroll (flag)                                                  | NOT REPRODUCED | scrollWidth == clientWidth (375). Current article titles are short; no fix applied. NewsHero H1 shares the same latent CMS long-word risk but did not reproduce, so left untouched (out of node scope). | none                                                                      |
| `/apply`                                      | one of "3 pages"                                                          | NOT REPRODUCED | 375 == 375                                                                                                                                                                                              | none                                                                      |
| `/contact`                                    | —                                                                         | NOT REPRODUCED | 375 == 375                                                                                                                                                                                              | none                                                                      |
| `/about`                                      | —                                                                         | NOT REPRODUCED | 375 == 375                                                                                                                                                                                              | none                                                                      |
| `/news/how-to-choose-the-perfect-location...` | reviewer's named article                                                  | NOT REPRODUCED | 375 == 375 (its title has no long word)                                                                                                                                                                 | none — but covered by the same `NewsArticle.tsx` fix                      |

Note: the homepage `brand-marquee` (width ~2519px) initially looked like the culprit but is correctly clipped by its parent `<section class="overflow-hidden">` and did NOT widen the document — ruled out via an overflow-x-clip ancestor check.

## Files changed

- `src/components/sections/Benefits.tsx` — added `min-w-0` to the text-column grid item; added `break-words` to the `<h2>`.
- `src/components/sections/CaseStudyQuotes.tsx` — added `min-w-0` to the quote-card `<li>`.
- `src/components/sections/NewsArticle.tsx` — added `break-words` to the article `<h1>`.

No `globals.css` changes were needed. No content/copy changes. NewsList.tsx and PublicLeadForm.tsx untouched. No admin code touched.

## RGR

- RED: `n9-measure.mjs` + `n9-diagnose.mjs` + `n9-news.mjs` — homepage overflow 30, case-studies 23, article overflow 104; offender selectors + ancestor-clip analysis identified the grid `min-width:auto` blowout and the H1 non-breaking word.
- GREEN: smallest class-only fixes (`min-w-0`, `break-words`). Re-run: every page `scrollWidth <= clientWidth`; article H1 wraps within viewport.
- REFACTOR: none needed — fixes are minimal Tailwind utility additions on the exact culprit elements.

## Gates

- `npx vitest run`: 422 passed / 422 (72 files). (Suite grew past the stated 416 from other workers; all green.)
- `npx tsc --noEmit`: exit 0.
- `npx eslint` on the 3 changed files: exit 0, clean.
- Browser gate: `n9-measure.mjs` → ALL PAGES PASS; `n9-news.mjs` → all 3 articles OK.

## Behavior preservation (screenshots in `shots/`)

- `n9-home-desktop-after.png`, `n9-casestudies-desktop-after.png`, `n9-article-desktop-after.png` (1280px) — layouts identical to before; two-column article, multi-column grids, hero all intact.
- `n9-home-mobile-after.png`, `n9-casestudies-mobile-after.png`, `n9-article-mobile-after.png` (375px) — no horizontal scroll; article H1 wraps the long word cleanly.
- `min-w-0`/`break-words` only change shrink/wrap behaviour when content can't fit a track — inert on desktop multi-column layouts and on short titles.

## Residual risk

- LOW. `NewsHero.tsx` (the `/news` index H1, clamp up to 8rem) has the same latent long-word overflow risk for future CMS titles but did not reproduce with current data; deliberately left for a globals/typography-owning node to avoid scope overlap. Worth a follow-up if very long single-word titles get published to the news index.

## Recommendation

Merge. All reviewer-flagged mobile overflows that reproduce are fixed at the source component with minimal class changes; desktop and copy preserved; full gates green.
