# N7 — Public News naming

## Status

COMPLETE (with browser-gate substitution — see Gates).

## Files changed

- `src/components/sections/NewsHero.tsx` — hero `<h1>` display copy "Blog" → "News".
- `src/components/sections/NewsArticle.tsx` — `/news` breadcrumb `<Link>` label "Blog" → "News".
- `src/components/sections/NewsHero.test.tsx` — NEW. Asserts hero heading is `News`, not `Blog`.
- `src/components/sections/NewsArticle.test.tsx` — NEW. Asserts `/news` breadcrumb label is `News`, not `Blog`.

## RGR

- RED: 4 new tests failed against the old "Blog" copy (hero `>Blog</h1>`, breadcrumb `>Blog</a>`).
- GREEN: changed the two display strings only; all 4 pass.
- REFACTOR: none needed — single-token display-copy change; followed existing colocated `renderToStaticMarkup` test pattern (mirrors `AdminShell.test.tsx`).

## Gates

- `npx vitest run` (full suite): 408 passed. Pre-existing unrelated failure in
  `src/components/forms/PublicLeadForm.test.ts` (another worker's file, NOT in my scope).
  Verified pre-existing: in isolation it fails identically (1 fail / 5 pass) WITH and WITHOUT my
  change stashed. My new section tests: 4/4 pass.
- `npx tsc --noEmit` (after `rm -rf .next`): one error, in
  `src/app/blog/[slug]/page.test.ts` (`RedirectType` used as type) — that file is the blog route
  owned by another worker, outside my scope. Zero type errors in my four files
  (`grep NewsHero|NewsArticle` is empty).
- `eslint` on all four of my files: clean (exit 0).
- Browser gate: BLOCKED by concurrent worker. The shared Next 16 dev server on :3000 is in a
  global 500 state — `/`, `/news`, and even a nonexistent route all return 500, i.e. a root
  layout/config/middleware crash from another active worker, not from my display-string edit.
  Next 16's project-wide single-instance lock prevents starting a second server without killing
  the other worker's process, so a live screenshot/curl was not safely achievable.
  SUBSTITUTE EVIDENCE (real DOM render of the actual components via `renderToStaticMarkup`,
  saved as artifacts):
  - `plans/ux-review-fixes/agent-runs/shots/n7-news-hero.html` →
    `<h1 ...>News</h1>` (uppercased by CSS `uppercase`, matching prior casing in code).
  - `plans/ux-review-fixes/agent-runs/shots/n7-news-article.html` →
    breadcrumb `<a ... href="/news">News</a>`.
  - `grep -ci ">blog<"` returns **0** on both rendered files (the exact curl-gate assertion,
    run against the components' real rendered markup rather than the broken server).

## Leftover "Blog" list (intentionally left)

Grep over `src/components/sections src/components/site src/lib/content src/app` (excluding admin
paths and the `/blog` route folder):

- `src/app/admin/news/*` — admin-internal labels ("Blog and news", "Blog CMS", "New blog post").
  OUT of scope per D5.
- `src/components/admin/AdminShell.test.tsx` (+ AdminShell nav) — admin "Blog and news" nav label.
  OUT of scope.
- `src/app/blog/[slug]/page.tsx` — the `/blog` route folder itself, owned by another worker.
  Excluded by instruction.
- `src/app/admin/pages/redirects/page.tsx:67` — input placeholder `"/blog/new-page"` (admin UI,
  example redirect path, not public News copy). Left.
- `src/app/admin/pages/actions.ts:120` — server-side reserved-slug regex `(resources|blog|landing|...)`.
  Functional path allowlist, not display copy. Left.
- `src/app/admin/pages/actions.test.ts` — test for the above. Left.

## Behavior preservation

Only two visible text strings changed. No structure, routing, props, styling, classes, or logic
touched. Hero/article layout, share rail, sidebar, headings, reading time, category label all
unchanged. The breadcrumb still links to `/news` (href unchanged); only its label text changed.

## Residual risk

Low. The only un-run check is the live-server screenshot/curl, blocked by a concurrent worker's
global 500 — substituted with real-component DOM render proof asserting the identical conditions.
Recommend a re-run of `curl -s http://localhost:3000/news | grep -ci ">blog<"` (expect 0) once the
shared dev server is healthy again.

## Recommendation

Accept. Change is correct, minimal, type/lint/test clean on my files, and proven via real DOM
render. Public News naming (D5) satisfied for the two in-scope public surfaces.
