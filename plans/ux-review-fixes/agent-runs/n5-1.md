# N5 — Inline form validation + "Not sure yet" options

**Status:** COMPLETE (all gates green)

## Files changed

- `src/components/forms/PublicLeadForm.tsx` — added `noValidate`; added "Not sure yet"
  to the three apply qualification selects (business_stage, budget, timeline); added an
  optional `initialState` prop (test seam only); upgraded the error `ActionMessage` to
  `role="alert"` / `aria-live="assertive"`; capture submitted field values in the submit
  wrapper and re-seed each field's `defaultValue` so values survive a failed submit.
- `src/components/forms/PublicLeadForm.test.ts` — added tests: "Not sure yet" appears once
  per qualification select; `novalidate` present; inline errors + `aria-invalid="true"`
  render from a pre-populated error state.
- `src/app/lead-action-handler.test.ts` — NEW. Empty apply payload returns per-field
  errors for every required field (fullName, email, stateRegion, businessStage, budget,
  timeline); "Not sure yet" clears the apply qualification requirement in validation.

**Select options module:** there is no separate options module — the apply select option
lists are defined inline in `PublicLeadForm.tsx`. The server schema (`leads.ts`, untouched)
treats these as free-text via `optionalText`, so "Not sure yet" is accepted without any
zod enum change.

## RED / GREEN / REFACTOR

- RED: component tests for "Not sure yet" count and error-state rendering failed (form had
  no `noValidate`, no option, no `initialState` seam). Handler tests passed immediately,
  confirming the server already returns per-field errors and accepts "Not sure yet".
- GREEN: added `noValidate`, the option, `initialState`, and the value-preservation wiring.
- REFACTOR: tightened the "Not sure yet" assertion to match the option text node (`>Not
sure yet<`) rather than a bare substring that double-counts the value attribute.

## Gates

- Full suite: `npx vitest run` → 70 files, **413 passed** (baseline 400 + my 5 + other
  workers' new tests).
- Types: `npx tsc --noEmit` → exit 0.
- Lint: `npx eslint` on the three changed files → exit 0.
- Browser (dev server on :3000 was occupied by another worker returning 500; ran my own on
  :3210):
  - (a) /apply empty submit → `form.noValidate === true`; `[aria-invalid="true"]` count = 6
    (≥3); `[role="alert"]` text = "Check the highlighted fields and try again."; 6 inline
    `p.text-red-600` errors (Name, email, State, Business stage, Budget, Timeline); no native
    bubbles. Typed "Preserved Name", re-submitted empty → value preserved.
  - (b) Filled all fields, "Not sure yet" in all three selects → redirected to
    `/thank-you-for-applying`.
  - Contact intent regression: empty submit shows 2 inline errors (no bubbles); full submit
    shows the Wave-1 success panel ("Message sent / The team will follow up at <email>").
  - Screenshots: `plans/ux-review-fixes/agent-runs/shots/n5-empty-errors.png`,
    `n5-value-preserved.png`, `n5-not-sure-redirect.png`, `n5-contact-panel.png`.

## Created test emails (delete nothing)

- `test+uxfix-n5-1781065315516@example.com` (apply, run 1)
- `test+uxfix-n5-1781065449608@example.com` (apply, run 2)
- `test+uxfix-n5-contact-1781065475323@example.com` (contact)
- `test+uxfix-n5-contact-1781065494516@example.com` (contact)
- `test+uxfix-n5-contact-1781065509222@example.com` (contact)

## Behavior preservation

- Server-side zod validation in `lead-action-handler.ts` → `submitLead` still rejects
  invalid payloads; the handler's `{success/error}` contract is unchanged.
- `leads.ts` and `lead-action-state.ts` untouched (verified via `git diff --name-only`).
- Wave-1 success flows intact: apply → `router.push(/thank-you-for-applying)`; contact →
  in-place success panel via `resolveLeadSuccessTransition`. Both confirmed in-browser.

## Residual risk

- Value preservation relies on capturing FormData in the submit wrapper and re-seeding
  uncontrolled `defaultValue`. It restores values React would otherwise reset after a form
  action; it does not interfere with success (form unmounts to redirect/panel) or with the
  hidden attribution inputs (which already carry fixed values).
- The `initialState` prop is a test-only seam; production callers never pass it, so the
  live default (`initialLeadActionState`) is unchanged.

## Recommendation

Integrate. All repo, browser, and boundary gates pass; contact intent and both Wave-1
success flows verified preserved.
