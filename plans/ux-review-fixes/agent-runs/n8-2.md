# Agent Run: N8 attempt 2 (orchestrator integration fix)

Status: DONE
Worker: orchestrator (integration pass)

## Root cause of attempt-1 live failure

Worker n8's page-level fix was correct but could never execute: `src/proxy.ts`
intercepts every `/blog/:path*` request as a builder route — no published
builder page at the path → `notFoundResponse()` at the proxy, before the app
route runs. Two layers masked it: (1) generateMetadata's notFound, (2) the
proxy. The worker's write boundary explicitly excluded middleware/proxy, so
the boundary itself was wrong; orchestrator expanded the node scope.

## Changes (attempt 2, on top of attempt 1)

- src/app/blog/[slug]/page.tsx — redirect branch mirrored into generateMetadata
  (Next docs confirm redirect()/notFound() are callable there); shared local
  helper redirectLegacyNewsSlug().
- src/proxy.ts — in the isBuilderRoutePath branch, when no published builder
  page exists at /blog/{slug} and the slug is a published news post, return 308
  to /news/{slug}. Builder page still wins; junk slugs still 404.
- src/proxy.test.ts — 3 new tests (RED proven: redirect case failed 404→expected 308).

## Evidence

- RED: vitest proxy.test.ts — 1 failed (redirect case) before proxy change.
- GREEN: proxy.test.ts 7/7; full suite 416/416; tsc clean.
- Boundary (live): /blog/how-to-choose-... → 308 → /news/... → 200 followed;
  /blog/junk-zzz → 404; /news direct → 200.

## Recommendation

DONE.
