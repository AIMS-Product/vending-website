# N11 — Admin P2 sweep

**Status:** COMPLETE — 4 fixed, 0 not-reproduced, 0 deferred (3 excluded per brief).

Core invariant preserved: admin list/editor functionality unchanged; all changes are
additive a11y / copy / affordance fixes. No Publish clicked, no users touched.

## Scope derivation

Extracted every `band === "P2"` row in `reports/ux-persona-review/final-findings.json`
whose `page` starts with `/admin` → **7 rows**. Applied brief exclusions
(sidebar News link, Quick Tour, Redirects, row-menu delete/duplicate).

## Disposition table

| cid  | page             | finding                                                                            | reproduced?                                       | disposition + evidence                                                                                                                                                                                                                               |
| ---- | ---------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C054 | /admin/news      | aria-prohibited-attr (serious) on `.text-slate-300`                                | YES (live axe)                                    | **FIXED.** Disabled pagination rendered `<span aria-disabled aria-label>` — `aria-label` prohibited on a span with no role. Replaced with `<button disabled aria-label>`. Live axe: 1 → 0.                                                           |
| C055 | /admin/media     | color-contrast (serious) `.mr-1`, `.space-y-2 > p` + heading-order (moderate) `h3` | YES (live axe)                                    | **FIXED.** `text-slate-400` (#90a1b9, 2.63:1) → `text-slate-500` (AA) on the "Type"/"Tags" filter labels and the "No tags" caption. Added `sr-only` section `<h2>Media assets</h2>` before the asset grid so hierarchy is h1→h2→h3. Live axe: 3 → 0. |
| C144 | /admin/pages     | "Schedule failed" workflow filter shown with no explanation                        | YES (curl: no explanatory title present)          | **FIXED.** Added a marketer-facing `description` to every workflow filter, surfaced as a `title` tooltip on each chip and as a visible muted helper line below the filter row whenever a non-"all" filter is active. Test-verified + screenshot.     |
| C105 | /admin/news/new  | Save draft & Publish are identical full-width blue buttons                         | YES (source: both used `adminPrimaryButtonClass`) | **FIXED.** Save draft → `adminSecondaryButtonClass` (white outline); Publish stays the single solid primary. Test-verified + screenshot.                                                                                                             |
| C021 | /admin/news      | No Blog/news sidebar link                                                          | n/a                                               | **EXCLUDED** (brief: sidebar News link already fixed). Confirmed present — AdminShell.test asserts "Blog and news" link to /admin/news.                                                                                                              |
| C022 | /admin/pages/new | Quick Tour overlay blocks panel                                                    | n/a                                               | **EXCLUDED** (brief: Quick Tour already fixed).                                                                                                                                                                                                      |
| C141 | /admin/news      | No row-level delete/archive                                                        | n/a                                               | **EXCLUDED** (brief: row-menu delete/duplicate already fixed/closed).                                                                                                                                                                                |

## Files changed

- `src/components/admin/AdminPaginationLink.tsx` (NEW) — shared pagination control; disabled state is a `<button disabled>`. De-duplicates 3 identical local copies.
- `src/components/admin/AdminPaginationLink.test.tsx` (NEW) — 2 tests.
- `src/components/admin/NewsEditorForm.test.tsx` (NEW) — 1 test.
- `src/app/admin/news/page.tsx` — use AdminPaginationLink; remove local PaginationLink.
- `src/app/admin/media/page.tsx` — use AdminPaginationLink; remove local PaginationLink; darken 2 filter labels; add sr-only section h2.
- `src/app/admin/pages/page.tsx` — use AdminPaginationLink; remove local PaginationLink; add filter descriptions + title + active-filter helper line. (AdminPagesActions header block untouched — confirmed.)
- `src/app/admin/pages/page.test.ts` — 1 test (Schedule failed explanation).
- `src/components/admin/MediaLibraryManager.tsx` — darken "No tags" caption to AA.

## RGR per fix

- **C054:** RED `AdminPaginationLink.test.tsx` (component absent) → GREEN new component with `<button disabled>` → REFACTOR replaced 3 duplicated `PaginationLink` defs across news/pages/media with the shared one.
- **C055:** RED = live axe (2 contrast + 1 heading-order on /admin/media) → GREEN className darken + sr-only h2 → axe re-run 0.
- **C144:** RED `page.test.ts` "explains what the Schedule failed filter means" (no title) → GREEN filter descriptions + title + helper line → screenshot.
- **C105:** RED `NewsEditorForm.test.tsx` (Save draft == primary) → GREEN Save draft → secondary → screenshot.

## Gates

- **Repo:** `npx vitest run` → 74 files, **426 passed** (baseline 416; +4 new + others' adds). `npx tsc --noEmit` → exit 0. `npx eslint` on all changed files → exit 0.
- **Browser (axe, live :3000):** before → after node counts:
  - /admin/news: aria-prohibited-attr 1 → **0** (total violations 0); disabled "Previous page" now `<button>` (was `<span>`).
  - /admin/media: color-contrast 2 + heading-order 1 → **0** (total 0).
  - /admin/pages: total violations **0** (was 0 for axe; C144 was a copy gap, fixed + screenshotted).
- **Screenshots:** `plans/ux-review-fixes/agent-runs/shots/n11-{news-new-buttons,pages-schedule-failed,media-contrast,news-pagination}.png`.

## Behavior preservation

- Pagination: same hrefs, labels, icons, enabled-state styling; only the disabled element type/markup changed (span→button) — non-navigable in both, now with a native disabled state.
- Workflow filters: same values/links/active styling; added tooltip + optional helper line only.
- News editor: same form, intents (`save`/`publish`), and submit behavior; only Save draft's visual weight changed.
- Media: only text color and an sr-only heading; no layout/logic change.

## Residual risk

Low. The three media filter labels share `.mr-1`; I darkened both visible group labels ("Type", "Tags"). The sr-only h2 is invisible. No data, auth, or publish paths touched. Temp axe/screenshot scripts were removed from the repo root.

## Recommendation

Land as-is. The shared `AdminPaginationLink` removes a recurring a11y defect at the source, so the prohibited-attr violation cannot reappear on any of the three admin lists.
