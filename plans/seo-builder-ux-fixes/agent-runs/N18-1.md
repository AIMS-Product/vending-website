# Agent Run: N18 attempt 1

Status: DONE

## Scope (files changed)

String/label changes only — zero logic.

- `src/app/admin/pages/redirects/redirect-status-labels.ts` — NEW. Shared words-first status vocabulary (`REDIRECT_STATUS_OPTIONS`, `redirectStatusLabel`).
- `src/app/admin/pages/redirects/redirect-status-labels.test.ts` — NEW. Pins the words-first labels + no-bare-number invariant.
- `src/app/admin/pages/redirects/RedirectCreateForm.tsx` — options now map over the shared list (same labels, single source).
- `src/app/admin/pages/redirects/RedirectRow.tsx` — table cell shows `redirectStatusLabel(...)` instead of a bare code; edit-select options use the shared list.
- `src/components/admin/seo-page-editor/BlockSettingsFields.tsx` — "Eyebrow" → "Overline (eyebrow)" (6 labels); hero "CTA" → "Call to action (CTA)" (first use).
- `src/components/admin/seo-page-editor/BlockInlineEditor.tsx` — in-canvas eyebrow accessible names "Eyebrow" → "Overline (eyebrow)" (sr-only + aria-label + proof label).
- `src/components/admin/seo-page-editor/MediaBlockCanvasEditors.tsx` — hero eyebrow accessible names "Eyebrow" → "Overline (eyebrow)".

## String change table (before → after → file)

| Before                                                                             | After                                                                                                     | File                        |
| ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | --------------------------- |
| `label="Eyebrow"` (rich_text, hero, proof — 6 OptionalBlockField/TextInput labels) | `label="Overline (eyebrow)"`                                                                              | BlockSettingsFields.tsx     |
| hero `label="CTA"`                                                                 | `label="Call to action (CTA)"`                                                                            | BlockSettingsFields.tsx     |
| `<span class="sr-only">Eyebrow</span>` / `aria-label="Eyebrow"` (rich_text canvas) | `Overline (eyebrow)`                                                                                      | BlockInlineEditor.tsx       |
| proof `label="Eyebrow"`                                                            | `label="Overline (eyebrow)"`                                                                              | BlockInlineEditor.tsx       |
| `<span class="sr-only">Eyebrow</span>` / `aria-label="Eyebrow"` (hero canvas)      | `Overline (eyebrow)`                                                                                      | MediaBlockCanvasEditors.tsx |
| table cell bare `{status_code}` (e.g. `301`)                                       | `Permanent move (301)` etc. via `redirectStatusLabel`                                                     | RedirectRow.tsx             |
| edit `<option>301/302/307/308</option>` (bare codes)                               | `Permanent move (301)` / `Temporary move (302)` / `Temporary redirect (307)` / `Permanent redirect (308)` | RedirectRow.tsx             |
| inline `<option>` words-first (already correct)                                    | now mapped from shared `REDIRECT_STATUS_OPTIONS` (identical labels)                                       | RedirectCreateForm.tsx      |

Identifiers deliberately NOT changed (code, not copy): `field="eyebrow"`, `field="cta"`, `eyebrowInputClass`, prop keys (`eyebrow`, `ctaLabel`, …), `name="statusCode"`, `<option value>` codes.

## Items checked and found ALREADY COMPLIANT (no churn)

- Pages list **workflow filters** ("All metadata", "Needs review", "Updating", "Needs links", "Metadata issues", "Scheduled", "Schedule failed") and **status filters** ("All"/"Drafts"/"Published"/"Archived") are already words-first (N7). Left as-is per "align, don't churn".
- `seo-pages-status-labels.ts` already uses marketer-facing labels (N7). Untouched.
- `RedirectCreateForm` already had words-first option text (N5); I only routed it through the shared module so it can't diverge from the row.
- BlockPicker / Shell: no raw schema jargon in user-facing strings.

## RGR Evidence

- RED-ish: added `redirect-status-labels.test.ts` pinning the NEW words-first labels and asserting `redirectStatusLabel` never returns a bare number — this fails loudly if anyone reverts to codes. (No pre-existing test pinned the old "Eyebrow"/bare-code copy, so there was no stale assertion to flip; the new test is the guard.)
- GREEN: applied the renames; new test passes (3/3); full suite stays green.
- REFACTOR: extracted one shared redirect-status vocabulary used by both the create form and the row, eliminating the duplicate inline option lists.

## Gates

- Repo:
  - `npx tsc --noEmit` → clean.
  - `npx eslint` on all 7 changed/new files → clean.
  - `npx vitest run` (full) → 93 files, 564 tests, all passed (was 553; +3 mine + sibling-node growth). No stale-copy assertion broke.
- Browser (Playwright, shared dev server PORT 3001):
  - Redirects create form options: `["Permanent move (301)","Temporary move (302)","Temporary redirect (307)","Permanent redirect (308)"]`. Screenshot `n18-screens/redirects.png`.
  - Redirect ROW (created a throwaway `… 308` redirect to get live data): table cell rendered **"Permanent redirect (308)"**; row edit-select options were the four words-first labels. Screenshots `redirects-with-row.png`, `redirects-row-edit.png`. **Throwaway redirect deleted afterward** (two-step Delete → "Delete redirect"); table back to empty state — verified, nothing left behind.
  - Editor block settings: "Overline (eyebrow)" label present; canvas in-place eyebrow accessible name also "Overline (eyebrow)". Screenshots `block-settings.png`.
  - Pages list filters: `pages-list-filters.png` (already words-first, unchanged).
- Boundary: n/a.

## Panel copy handoff (HARD-FENCED files — for N12 / n1)

These user-facing strings live in worker-n1's active files; I did NOT edit them. Recommended renames per admin-studio.md "Language":

- **"Slug" → "URL ending (slug)"** — the only "Slug" field label lives in `SeoPublishPanel.tsx` (slug field label + any slug help text). Not in any allowed file.
- Any **"Governance"** / raw-schema expansions in `SeoPublishPanel.tsx` / `ScheduleStatusCard.tsx` / `editor-publish-confirmation.ts` (e.g. technical SEO field labels) should get the same words-first pass when N12 touches the panel.

## Service-string handoff (worker-n5 files)

- None required for N18. The redirect validation message ("Choose a supported redirect status.") in `actions.ts` is already plain-language; left untouched (off-limits anyway).

## Residual Risk

- Minimal. Pure label text + one shared presentational helper; `<option value>` codes and all `name=`/`field=` identifiers are unchanged, so form submission and validation are unaffected (full suite confirms).

## Recommendation

Accept. Eyebrow/CTA field labels and redirect status copy are now words-first and consistent (redirect vocabulary shared by form + row); list filters were already compliant; the "Slug" rename and any panel jargon are correctly handed off to N12 because they live in n1's fenced files. Repo and browser gates green; throwaway test data cleaned up.
