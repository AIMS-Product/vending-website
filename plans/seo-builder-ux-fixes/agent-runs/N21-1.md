# Agent Run: N21 attempt 1

Status: DONE

## Scope (files changed)

- `src/lib/page-builder/datetime-format.ts` — NEW. The single Pacific-anchored `formatPacificDateTime(iso)` (month/day/hour/minute + `timeZoneName: "short"`, `timeZone: SCHEDULED_PUBLISH_TIME_ZONE`). Neutral lib home so both `src/components/...` and `src/app/...` can depend on it.
- `src/components/admin/seo-page-editor/editor-time-format.test.ts` — NEW. Format-consistency + Pacific-anchor + divergence-reproduction test.
- `src/app/admin/pages/[id]/revisions/revision-display.ts` — moved the formatter out to the neutral file; now `export const formatRevisionDateTime = formatPacificDateTime` (re-export under the original name). Import block changed from `SCHEDULED_PUBLISH_TIME_ZONE` to `formatPacificDateTime`. No logic change to the other exports.
- `src/components/admin/seo-page-editor/SeoPageEditorTopRail.tsx` — removed the local `formatRailTime` (zone-less `toLocaleTimeString`); the "Saved automatically · …" hint now calls `formatPacificDateTime`.
- `src/components/admin/seo-page-editor/MobileEditorActionBar.tsx` — removed the local `formatBarTime`; same hint now calls `formatPacificDateTime`.

The two revision-surface CONSUMERS (`SeoPageRevisionPanel.tsx`, `[revisionId]/page.tsx`) and the revision test needed NO changes — the re-export preserves `formatRevisionDateTime`'s name and signature.

NOT touched: SeoPublishPanel.tsx, useSeoPageEditorController.ts, SeoPageEditorHiddenFields.tsx, editor-form-data.ts, schedule-status.ts (n1), services/migrations (n5), walkthrough files.

## Relocation note (layering)

The formatter previously lived in the revisions route folder, and `SeoPageRevisionPanel.tsx` (a component) already imported it from there — a component reaching into `src/app/.../revisions/`. Rather than extend that wrong-direction dependency to two more editor components, I moved the formatter to a neutral `src/lib/page-builder/datetime-format.ts`. `revision-display.ts` re-exports it, so the existing revision imports stay valid and unchanged. Net layering is improved, not just preserved.

## RGR Evidence

- RED/divergence proof: `editor-time-format.test.ts` includes a case that pins the runtime to `America/New_York` and shows the OLD rail/mobile approach (`toLocaleTimeString` with NO timeZone) yields a zone-less, date-less string that does NOT equal `formatPacificDateTime` for a fixed instant (`2026-06-10T22:45:00Z`). This is the exact divergence N21 removes.
- GREEN: after routing both editor displays through `formatPacificDateTime`, the consistency assertion `formatPacificDateTime(iso) === formatRevisionDateTime(iso)` passes, and the output carries `3:45 PM` + a `PDT/PST` label.
- REFACTOR: extracted one shared formatter; re-exported from the revision module to avoid touching consumers; deleted both private editor formatters (no third formatter exists).

## Gates

- Repo:
  - `npx tsc --noEmit` → clean.
  - `npx eslint` on all 5 changed/new files → clean.
  - `npx vitest run` (full) → 90 files, 537 tests, all passed (was 533; +4 from the new test). The existing `revision-display.test.ts` still passes through the re-export.
- Browser (Playwright, shared dev server PORT 3001):
  - Loaded the editor, triggered autosave with a content-NEUTRAL edit (typed a space, immediately backspaced — saved content unchanged), and captured the rail hint: **"Saved automatically · Jun 10, 1:34 AM PDT"** — Pacific zone label + date + time, matching the revision surfaces' format. Screenshot: `n21-screens/rail-saved-automatically-pt.png`.
  - (The wall-clock value reflects the test browser's clock; the point is the FORMAT — `Mon D, h:mm AM/PM PDT` — now matches revisions, vs the prior bare `h:mm AM/PM`.)
  - No throwaway row created: the edit was reverted in place and the autosave is the page's normal behavior; nothing to clean up.
- Boundary: n/a.

## Behavior Preservation

- Revision surfaces: identical output (same formatter, re-exported under the same name); their tests pass unchanged.
- No timezone handling changed anywhere else — `SCHEDULED_PUBLISH_TIME_ZONE` remains the single source of the zone; only the formatter's location moved and two editor displays adopted it.

## Residual Risk

- Very low. One pure formatter, now shared; the editor change is purely presentational (the hint gains a date + PT zone label). The mobile-bar hint already only renders at narrow widths (unchanged).

## Handoff Notes

- The N16 handoff note in `revision-display.ts` (which flagged the editor's `formatRailTime` as a future unification) is now resolved and the comment replaced with the relocation note.
- Evidence script kept: `plans/seo-builder-ux-fixes/agent-runs/n21-verify.mjs` (PORT 3001; optional page-id argv).

## Recommendation

Accept. Rail and mobile-bar save times now render in the same Pacific-anchored format as the revision surfaces, proven by a fixed-instant unit test and a live screenshot; the formatter is a single shared source in a neutral location; repo and browser gates are green; no other timezone behavior changed.
