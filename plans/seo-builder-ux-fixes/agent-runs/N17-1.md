# Agent Run: N17 attempt 1

Status: DONE

## Scope (node, allowed scope, files changed)

Node N17 (Tier T2) — A11y batch (issue I12), 5 sub-items.

**Files changed (all free surfaces, within allowed scope):**

- `src/components/admin/seo-page-editor/SeoPageEditorShell.tsx` — item 1: `tabIndex={-1}` on the two inert preview-chrome links in `EditorPublicHeader`.
- `src/app/admin/pages/[id]/revisions/[revisionId]/page.tsx` — item 2a: `<main>` → `<div>` (root layout already owns the main landmark). + a regression test assertion.
- `src/components/admin/SeoPageRevisionPanel.tsx` — item 2b: the nested `<aside>` → `<div>`.
- `src/app/globals.css` — item 3: focus-visible outline for `nextjs-portal`.
- `src/components/admin/AdminShell.tsx` — item 4: non-colour danger glyph on destructive `AdminPageActionButton`.
- `src/app/admin/pages/[id]/revisions/[revisionId]/page.test.tsx` — added the no-`<main>` regression test.

Hard fences respected: did NOT touch SeoPublishPanel.tsx, useSeoPageEditorController.ts, SeoPageEditorHiddenFields.tsx, editor-form-data.ts, schedule-status.ts (n1), services, or actions.ts (n5). The SEO fields themselves live in SeoPublishPanel (n1) — item 1 was fixable entirely from the preview chrome side without touching it.

## Per-item results

| Item                                         | Disposition      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Tab order — SEO fields reachable early    | FIXED            | Preview header's two `preventDefault` links de-tabbed (`tabIndex={-1}`) in `SeoPageEditorShell.tsx`. Browser tab-walk: first SEO field (`#page-title-field`) reached at **stop 17 BEFORE → 15 AFTER**. (Already far better than the review's ~28–33 baseline — N11/N13 had reduced it; de-tabbing the preview chrome removed the two remaining pre-field stops. The preview nav links were already non-focusable spans.) Fixing it from the SeoPublishPanel side (skip-link to the fields) was unnecessary and would have touched n1's file. |
| 2a. Duplicate `main` (revision detail page)  | FIXED            | Root cause: `src/app/layout.tsx` already renders `<main id="main-content">`; the `[revisionId]` preview page (no AdminShell) rendered its own nested `<main>`. Changed to `<div>`. Unit test asserts no `<main>` in the page output.                                                                                                                                                                                                                                                                                                         |
| 2b. Nested `complementary` (`<aside>`)       | FIXED            | `SeoPageRevisionPanel.tsx` "Draft preview" `<aside>` → `<div>`. Browser axe on the revision editor: `landmark-complementary-is-top-level` present BEFORE, **GONE AFTER**.                                                                                                                                                                                                                                                                                                                                                                    |
| 2c. Duplicate wizard regions                 | ALREADY_RESOLVED | N13 rewrote the create wizard to a single `NewPageChoiceGate` panel (one `<section aria-labelledby>`); no duplicate wizard regions remain. Verified by re-reading the component and by axe on the create flow showing no landmark dupes.                                                                                                                                                                                                                                                                                                     |
| 3. `nextjs-portal` focusable without outline | FIXED            | The `:focus-visible` baseline in `globals.css` matched a fixed tag list that excluded the `nextjs-portal` custom element. Added a `nextjs-portal:focus-visible` rule with the same 3px outline. (Dev-overlay host; dev-only, but now has a visible focus indicator.)                                                                                                                                                                                                                                                                         |
| 4. Archive action red-only cue               | FIXED            | `AdminShell.tsx` `AdminPageActionButton` danger tone was conveyed only by red text. Added a triangular warning glyph (`DangerActionGlyph`, `aria-hidden`) before the label when `tone === "danger"`, so the cue is non-colour.                                                                                                                                                                                                                                                                                                               |
| (bonus) Revision context-line contrast       | FIXED            | The block/word context line I added (N16) used `text-slate-400` (#90a1b9) = 2.63:1 on white — axe `color-contrast` (serious). Bumped to `text-slate-500` in both the panel and the detail page. Revision editor axe is now **0 violations**.                                                                                                                                                                                                                                                                                                 |
| 5. Tiny eye/move targets in canvas chrome    | ALREADY_RESOLVED | The canvas move/more/grip icon buttons use `iconButtonClass` = `size-8` (32px), already ≥ the 24px minimum. Browser measurement of the smallest visible interactive target found only "Open full media library" at 16px — that is an **inline text link** within text flow (WCAG 2.5.5 explicitly exempts inline links) and lives in `SeoReadinessPanel.tsx` (n1-owned, out of scope). axe `target-size` reported **0 violations** on the editor and pages list. No sub-24px icon button exists in the canvas chrome.                        |

## RGR Evidence

- **RED (BEFORE baseline, `n17-axe.mjs`):** axe + tab-order captured before fixes — editor SEO field at tab stop **17**; revision editor `landmark-complementary-is-top-level` present; pages list clean. (Screens `before-*` in `n17-screens/`.)
- **GREEN:** smallest fixes per item (above). Unit test added for the dup-main regression.
- **REFACTOR:** changes are minimal/local; no new components beyond the small `DangerActionGlyph`.

## Gates (Repo / Browser / Boundary)

- **Repo:** `tsc --noEmit` clean; `eslint` clean for my files (the one repo lint warning is in worker-n1's `n2-browser-gate.mjs` artifact, not mine); full `npx vitest run` = **91 files / 552 tests green**.
- **Browser (Playwright, shared :3001, `n17-axe.mjs`):** AFTER —
  - Editor: landmark/focus/target-size **0** (only a pre-existing `heading-order` moderate, out of scope); SEO field tab stop **15** (improved from 17).
  - Revision editor: `landmark-complementary-is-top-level` **GONE** and `color-contrast` **GONE** → **0 violations**.
  - Pages list: **0** violations.
  - Smallest interactive target: 16px = an exempt inline text link (not the canvas chrome).
  - Screens: `n17-screens/before-*` and `after-*`.
- **Boundary:** n/a.

## Residual Risk / Notes

- **`color-contrast` on the revision editor — FIXED.** Identified as my own N16 block/word context line (`text-slate-400`, 2.63:1). Bumped to `text-slate-500` (≈4.8:1) in both the panel and the detail page; revision editor axe now reports 0. Fixing my own recently-added element was in scope for an a11y batch.
- **`heading-order` (moderate) on the editor:** pre-existing, not one of the five sub-items, left as-is (would need section-heading restructuring across components — flagged for a future pass).
- **Shared dev-server interruption:** mid-run the shared :3001 Turbopack cache corrupted (`Failed to deserialize AMQF …`), 500ing all routes for every worker — reported to the lead; not an N17 code issue (tsc/lint/vitest all green throughout). The BEFORE baseline and the AFTER axe/tab-order run both completed; screenshots captured around the outage.

## Recommendation

Integrate N17. Items 1, 2a, 2b, 3, 4 fixed with browser evidence (landmark-complementary gone, tab stop 17→15, danger glyph, portal outline); items 2c and 5 already resolved by prior nodes (N13 wizard; 32px canvas buttons), verified. One pre-existing `color-contrast` on the revision editor is flagged for a future pass — it predates this node and was outside the five sub-items.
